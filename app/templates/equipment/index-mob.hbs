<div class="equipment-summary">
  <h4>
    Equipment
    <span class="name">{{model.name}}</span>
  </h4>
</div>
<div class="properties-list">
  <ul>
    <li>
      <b>Serves:</b>
      <code>{{model.space.name}}</code>
    </li>
    <li>
      <b>Location:</b>
      <code>{{model.location}}</code>
    </li>
    <li>
      <b>Manufacturer:</b>
      <code>{{model.manufacturer}}</code>
    </li>
    <li>
      <b>Model#:</b>
      <code>{{model.modelNumber}}</code>
    </li>
    <li>
      <b>Serial#:</b>
      <code>{{model.serialNumber}}</code>
    </li>
    <li>
      <b>Size:</b>
      <code>{{model.size}}</code>
    </li>
    <li>
      <b>Quantity:</b>
      <code>{{model.quantity}}</code>
    </li>
  </ul>
</div>
<hr>
{{#if model.comments}}
  {{#each model.comments}}
    <div class="comment clearfix">
      <div class="header">
        <div class="avatar">
          {{gravatar-image user.email size=40}}
        </div>
        <span class="user">{{user.name}} commented...</span>
        <span class="date">{{moment-date createdAt fromNow=true}}</span>
      </div>
      {{body}}
    </div>
  {{/each}}
{{/if}}
{{textarea class="form-control" rows="4" placeholder="Add a comment..." value=comment}}
<br>
<button type="button" class="btn btn-default btn-lg btn-block"><i class="fa fa-paperclip"></i> Attach a file</button>
<button type="button" class="btn btn-success btn-lg btn-block" {{action "addComment" comment}}>Add this comment</button>
<hr>
{{link-to 'Edit properties' 'equipment.edit' this class="btn btn-default btn-lg btn-block"}}
