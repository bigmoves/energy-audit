<div class="container">
  <div class="bubble">
    <h3 class="bubble-title"><b>Buildings</b></h3>
    <ul class="bubble-list">
      {{#each}}
        <li class="bubble-list-item">
          {{#link-to 'building' this }}
            {{name}}
          {{/link-to}}
        </li>
      {{/each}}
    </ul>
  </div>
</div>
